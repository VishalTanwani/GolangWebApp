{{template "base" .}}
{{define "content"}}
<div class="container">
    <div class="row">
        <div class="col">
            <h1>this is about page</h1>
            <p>this came from handler {{index .StringMap "test"}}</p>
            {{if ne (index .StringMap "remote_ip") ""}}
                <p> your ip addres is {{index .StringMap "remote_ip"}}</p>
            {{else}}
                <p>i dont know ur ip address please visit <a href="/">home<a> page so i can see it</p>
            {{end}}
        </div>
    </div>
</div>
{{end}}